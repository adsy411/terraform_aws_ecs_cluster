# terraform_aws_ecs_cluster
terraform_aws_ecs_cluster

Step-1 
  `install aws-cli
   configure aws-cli`
Step-2
  `cd ./terraform_aws_ecs_cluster
  terraform init
  terraform plan -var-file=./secrets.tfvars
  terraform plan -var-file=./secrets.tfvars`
  
